{
  "name": "banana-game-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "install-all": "npm install -w apps/frontend",
    "dev": "npm run dev -w apps/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
